## What this changes

<!-- One or two sentences. Link the issue if there is one. -->

## Why

<!-- The problem this solves, or what it makes possible. -->

## How it was tested

<!-- The phone and Android version, and what you did on it. -->

## Checklist

- [ ] The branch starts from `mobile`, and this pull request targets `mobile`
- [ ] `npx tsc --noEmit`, `npx eslint src/ App.tsx __tests__/` and `npx jest` pass
- [ ] Changed Python compiles (`python -m py_compile`)
- [ ] The application id, signing keystore and version numbers are unchanged
- [ ] New dependencies, if any, are explained above
- [ ] The documentation in `docs/content/` is updated if behaviour changed
